//
//  Alert.swift
//  NewDesignApp
//
//  Created by Vivek SIngh on 24/10/24.
//

import Foundation
/*
func alert() {
    if Cart.shared.restuarant.mindelivery > Int(Cart.shared.getAllPriceDeatils().subTotal) {
        let alertController = UIAlertController(title: "Replace cart item?", message: "Your cart contains dishes from restaurantname. Do you want to discart the selection and add dishes from restaurantname?", preferredStyle: .alert)
        let OKAction = UIAlertAction(title: "Add", style: .default) { action in
            self.navigationController?.popViewController(animated: true)

        }
        let cancel = UIAlertAction(title: "Cancel", style: .cancel) { alert in
            
        }
        alertController.addAction(OKAction)
        alertController.addAction(cancel)
        OperationQueue.main.addOperation {
            self.present(alertController, animated: true,
                         completion:nil)
        }
    }
    
}
*/
